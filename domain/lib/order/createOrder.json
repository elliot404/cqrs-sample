{
  "title": "createOrder",
  "type": "object",
  "properties": {
    "id": {
      "type": "string"
    },
    "command": {
      "type": "string"
    },
    "payload": {
      "type": "object",
      "properties": {
        "text": {
          "type": "string",
          "pattern": "^([^0-9]*)$"
        }
      },
      "required": ["text"]
    }
  },
  "required": ["id", "command", "payload"]
}